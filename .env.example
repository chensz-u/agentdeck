# Optional: defaults to .agentdeck/data.json beneath the directory running AgentDeck.
# Use an absolute file path if local data should live elsewhere.
AGENTDECK_DATA_PATH=

# Reserved for a future Prisma migration. The current runtime does not generate or
# use a Prisma client, migrations, or this value.
DATABASE_URL="file:./dev.db"

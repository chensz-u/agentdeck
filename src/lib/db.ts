/**
 * Prisma has no generated client in this local-first MVP. Runtime persistence is
 * provided by `LocalRepository` until a generated client and migration workflow
 * are intentionally added.
 */
export const db = null;
